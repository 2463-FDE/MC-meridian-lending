# doc_path_lint_allow.txt — references that are CORRECTLY absent from this tree.
#
# scripts/check_doc_paths.sh fails on a backticked repo-path the tree does not
# contain. docs/kb.md's branch-layout section cites files that live on another
# branch on purpose: naming where the work sits is the section's job, and deleting
# the reference would make the doc less useful, not more correct. Those go here.
#
# One path per line, `#` starts a comment. Every entry needs a reason and the
# branch or PR that will resolve it — an entry with no stated exit is a bypass.
#
# An entry whose path STARTS resolving fails the run: delete the line when the
# branch merges. That is the only thing keeping this list from rotting.

# Week 5 payments package — spec-only, lives on feature/payments-week5 (PR #14).
# Delete these three when #14 merges.
docs/scoping-payments-week5.md
docs/spec-payments-week5.md
scripts/repro_double_charge.py
