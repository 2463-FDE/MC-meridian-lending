# Meridian Lending — Knowledge Base (entry point)

Compact map over work done so far. Detail lives in the linked files — this page just routes you there. Read this first; open a detail file only when its *read-when* hook matches your task.

**Weeks 1–10 have merged.** Week 5 and the *original* Week 6 deliverables are spec/ADR packages with no implementation behind them; weeks 4, 7, 8, 9 and 10 carry real code, and so does Week 6's follow-on authz fix — what stays unbuilt from Week 6 is maker-checker and the append-only ledger. **No merge state is recorded on this page.** The base tip, the full merged-PR ledger, the ADR file list and which CI jobs block are generated into `docs/state.md` by `scripts/gen_state.sh` — read that page for any number, and run `make kb` if it looks behind. Which PRs are *open* is not derivable from a checkout at all: query GitHub.

---

## 1. Orientation

- **Client:** Meridian Lending Co. — Dana, VP Lending Ops. Finance brownfield.
- **Repo:** `/Users/maha/Desktop/revature/MC-meridian-lending`.
- **Program:** FDE brownfield track. Weekly loop — read artifacts, find the *latent* problem under Dana's casual ask, ship one small increment, log the debt.
- **Stack:** microservices (gateway, origination, decision, kyc, disclosure, servicing) + RAG eval harness + LLM client wrapper. See `ARCHITECTURE.md`.

### Branch layout (important)
- `main` — the client's current state; its tip is generated into `docs/state.md`, never asserted on this page. **It is no longer the brownfield baseline:** weeks 1–10 have merged, so log redaction, the secret purge, servicing route authz, the Decimal disclosure path, the settlement reconciliation control, the D19 payment-idempotency claim, `search_policy`, native tool calling on the officer assistant loop and its trace surface all ship here. **§3 now covers weeks 1–10** — see the Week 9 and Week 10 entries below. The brownfield debt that is still real on `main`: float money math outside the disclosure compute path (D2), the plaintext PAN in the `payments` table (D13b — the CVV column was deleted by migration `db/migrations/0020_payments_drop_cvv.sql` on 2026-08-24, D13a, held by the blocking `no-sad-gate` — the only CI job with a postgres service, because that migration is hand-applied and re-runnability is a question only a real server answers), bureau and processor keys purged from the tree but never rotated (D1). **D32 is no longer on that list either:** both `adjust_balance` and `waive_fee` are now atomic (a compare-and-set and a stored-value decrement respectively), held by the blocking `atomic-apply-gate` — see `docs/debt-log.md` D32. **D20 is no longer on that list either:** `audit_logs` now carries the same append-only/no-truncate trigger pair `decision_events` has (migration `db/migrations/0022_audit_logs_append_only.sql`), and `origination-service` — the table's only writer — refuses readiness unless both triggers are present and enabled, held by the blocking `db-readiness-gate`. **D3 is no longer on that list:** `apply_payment` became one atomic statement in PR #77 (`d649bc6`, merged `ceda4e2`, migration 0019, `payment_applications` `UNIQUE (payment_id)`), held by the blocking `atomic-apply-gate`. D19 did not fix it — an idempotency key stops one key being claimed twice, it does not make the balance mutation atomic. D3's decision record, ADR 0020, merged as PR #78 (`0d47601`), so cite both the code and the ADR as built. **Cite `main` (`git show main:<file>`) as the client's current state, never a feature branch** — and read `docs/debt-log.md` for which controls exist rather than assuming the baseline.
- `feature/*`, `feat/*`, `fix/*` — the user's own work on top. Sessions now keep **one `git worktree` per active branch** rather than switching the shared checkout, because several sessions run against this repo at once; the main checkout is often on someone else's branch and dirty (`make prove` needs a detached worktree, never stash).
- **This page's own correction** — docs/debt-log-d5-status, which absorbed chore/debt-log-status-rule by merge and ships as one PR (#52) for both. It corrects D5 from "in an unmerged PR" to Mitigated, adds the Debt-log status vocabulary to `CLAUDE.md`, and resyncs this page. Two doc-accuracy branches it retires without merging: docs/kb-week7-status (its week-7 correction was itself stale — it said D1 and D4 were unbuilt after both had landed) and, sequenced after this one because it needs the dangling references settled first, chore/untrack-status-tracker (untracks the tracker decoy, widens `scripts/check_doc_paths.sh`). **Whether any of those has merged is deliberately not stated here — query GitHub.** `fix/servicing-authz` (#32) merged 2026-08-13. *(Branch names here are un-backticked on purpose — `doc-path-lint` reads a backticked slash-bearing span as a repo path that never resolves.)*
- **Merged and done:** *(the five `docs/…` branch names below are un-backticked on purpose — `doc-path-lint` would read them as repo paths that never resolve)* `feature/llm-foundation-week1` (#1), `feature/pii-redaction` (#2), `feature/llm-client-week1` (#3), `security/purge-committed-secrets` (#4), `feature/rag-eval-week2` (#5), `feature/rag-eval-impl-week2` (#6), `feature/decision-assistant-week3` (#7), `feature/langsmith-tracing` (#8), `fix/redactor-ssn-separator-blindspots` (#9), `feat/apply-form-validation` (#10), `feat/assistant-ui-panel` (#11), `feature/disclosure-week4` (#12), `fix/dob-validation` (#13), `feature/payments-week5` (#14), `chore/prove-target` (#15), `chore/spec-diff-gate` (#25), docs/architecture-refresh (#26), docs/regulator-watch-2026-08-14 (#27), docs/week6-servicing-prompts (#28), docs/servicing-money-controls-adr-week6 (#29), docs/servicing-comprehension-week6 (#30), `feat/payment-reconciliation-week7` (#31).
- Local-only: `backup/security-pre-cleanup`, `demo/week1-4`, `wip/tracker`. **The canonical feature-status-tracker.md no longer lives on any branch** — it moved outside the repo to `~/.claude/projects/-Users-maha-Desktop-revature-MC-meridian-lending/feature-status-tracker.md` on 2026-08-13. `main` tracks a 1,996-byte 3-row decoy of it, so a checkout materializes the decoy — read the canonical path above, never the tracked file.

---

## 2. Where things live — read-when map

| Read when you need… | File |
|---|---|
| Full status of every feature (branch, spec, ADRs, test/teeth results, PR round) | the canonical tracker, which lives outside the repo — path in *Branch layout* above. The tracked feature-status-tracker.md in this tree is the 3-row decoy; do not read it for status. |
| What weeks 1–4 owe, the trainer scoreboard (score lives in that doc, not here), the merge sequence and the standing metrics | docs/goal-weeks1-4-closure.md (local-only) — living doc, `.html` twin + artifact `5c9ed224` | <!-- VOLATILE-OK: published artifact id, immutable — not a commit -->
| What merges when, in what order, and the nine-item feedback scoreboard | docs/goal-weeks1-4-closure.md (local-only) |
| The base tip, the merged-PR ledger, the ADR file list, which CI jobs block | `docs/state.md` — generated by `scripts/gen_state.sh`, never hand-edited. Run `make kb` if it looks behind. |
| Known security/compliance/arch debt + mitigation paths, one D-numbered entry each | `docs/debt-log.md` — the register itself is the range; do not restate it here |
| Why the officer assistant writes a telemetry row, what may go in a column, and how those rows are read back | `services/origination-service/app/assistant_runs.py`, `db/migrations/0021_assistant_runs.sql` — the module docstring and the DDL comment are the record; there is no spec or ADR, and that gap is a named exception with an owner in `scripts/spec_gate_map.txt`. The read back is officer-gated `GET /assistant/metrics` (`services/origination-service/app/main.py`), the table's only export boundary: counts and enum codes, never `application_id` or `trace_id`, and any value outside a vocabulary it knows is masked because `outcome` and `policy_band` carry no CHECK. Runbook: `docs/runbooks/operations.md`. |
| How LOS ↔ decision/servicing services talk (the seam) | `docs/los-lss-seam.md` |
| A design decision + its rationale | `adr/` (index below) |
| The spec a feature was built against (source of truth) | `docs/specs/ai-assistant-week1.md`, `docs/specs/rag-week2.md`, `docs/specs/decision-assistant-week3.md`, `docs/specs/disclosure-week4.md`, `docs/specs/payments-week5.md` |
| How a fuzzy client ask was reframed into the real problem | `docs/scoping/payments-week5.md` (Week 5 — the worked example) |
| To prove the payment double-charge is real, not customer confusion | `scripts/repro_double_charge.py` (run it; both defects reproduce) |
| The plan behind a stage/phase (pre-build design) | `docs/plans/stage1-ai-assistant.md`, `docs/plans/stage1-rag-week2.md`, `docs/plans/phase1-bedrock-pgvector.md`, `docs/reports/stage2-verification.md` |
| System architecture (services, data flow) | `ARCHITECTURE.md` (root), `docs/architecture.md` |
| Run/operate the stack locally | `docs/runbooks/operations.md` |
| Secret purge + key rotation history | `docs/security-remediation-2026-07.md` |
| Past adversarial review findings | docs/teeth-review-2026-07-10.md (local-only), `docs/reports/llm-client-code-review.md` |
| The adversarial red-team pass over the agent surface — its findings, which are closed, and which are open on `main` with no owner | `docs/red-team/2026-09-02.md` (newest file in that directory wins) |
| How review round-trips are run | `docs/process/review-roundtrip-playbook.md` |
| To resume a stopped session with no re-explaining | docs/handoffs/ (local-only, newest wins) |
| Correct-by-construction invariant catalogs (credential/atomicity/replay/crypto) to build in, not review out | `docs/build-invariants.md` |

### ADR index (`adr/`)
0001 record-architecture-decisions · 0002 single-db-shared-schema · 0003 store-card-data (**superseded by 0013**) · 0004 decompose-origination · 0005 llm-client-design · 0006 logging-redaction · 0007 rag-corpus-hygiene · 0008 retrievable-decision-records · 0009 decisioning-assistant-design · 0010 application-ownership-authorization · 0011 mandatory-kyc-before-decisioning · 0012 decimal-minor-units-and-externalized-rule-config (**Proposed**) · 0013 payment-idempotency-and-tokenization (**Proposed**, superseding 0003; Decision 1 built, Decision 2 half built, Decision 3 not built) · 0014 servicing-money-controls (**Proposed**) · 0015 settlement-reconciliation-as-a-control (**Accepted** once the control was real) · 0016 fair-lending-monitoring-computes-outside-the-platform (**Proposed**) · 0017 self-decision-anonymous-submission-gap (**Proposed**, not built) · 0018 interim-handling-of-a-double-charged-borrower (**Proposed**, partly built — schema rung and capture path built, held by the blocking `payment-idempotency-gate`) · 0019 policy-retrieval-on-the-assistant-loop (**Accepted**, built) · 0020 atomic-payment-application (**Accepted**, built, held by the blocking `atomic-apply-gate`) · 0021 agent-framework-and-root-trace (**Accepted**, built and written after the fact — the decision it records is why the officer loop runs on langgraph's prebuilt agent rather than LangChain v1's `create_agent`, and why the trace is hand-built; its root is `assistant.entry` at the route funnel, with the loop root inside it, so a refusal raised before the loop starts is traced — the gateway hop and the route's pre-funnel 403/422 refusals still are not) · 0022 offline-evaluator-for-the-support-test (**Accepted**, built — `rag_eval/evaluator.py`, wired into `rag_eval/run.py` and held by the blocking `rag-eval-gate`; it grades only when asked, so a run with no judge reports every model-graded axis as `not_evaluated` rather than as a pass) · 0023 applicant-ssn-at-rest (**Proposed**, partly built — Decision 1, which reduces internal exposure independently of retention, merged as #142 (`25157e4`): `applicants.ssn_last4`, the intake and recheck hop to kyc-service on last-4, and the purge CLI's placeholder interlock, held by the blocking `ssn-purge-gate` and a `db-readiness-gate` rung. Decision 2, the retention-contingent remediation, is not built — it waits on a client answer on retention duration, so the column itself is still plaintext and nothing has been purged). The ADR file list and its count are generated into `docs/state.md` — read the count there, and the file out of `main`, before citing either. Since the gate landed, every tracked `adr/*.md` and `docs/specs/*.md` must be mapped in `scripts/spec_gate_map.txt` or carry a line-leading `# EXEMPT:` with a reason, or the blocking `spec-diff-gate` job fails — a new ADR is never a docs-only change. Numbers are claimed at write time and can collide: 0015 was written as 0014 and renumbered when week 6 took that number the same evening.

---

## 3. Feature state

- **Week 1** — LLM client wrapper (timeout/retry/structured output/cost guard) + PII-redacting logging + LOS↔LSS seam map. **Merged** — PRs #1 (2026-07-07), #3 (2026-07-10); secret purge #4 (2026-07-10).
- **Week 2** — RAG retrieval eval harness + corpus-hygiene gate. **Merged** — PR #5 (2026-07-11, spec) + PR #6 (2026-07-15, implementation), after ~24 review-fix commits. Blocking `rag-eval-gate` on `main`.
- **Week 3** — single-agent decisioning assistant + append-only decision record. **Merged** — PR #7, 2026-07-19, after 12 review rounds (idempotency, internal-only service gating, schema-readiness health gates, SSN-fingerprint pepper, offer/boarding state guards). Blocking `decision-idempotency-gate`, `adr-0010-authz-gate`, `kyc-enforcement-gate`, `offer-guard-gate`.
- **Week 4** — auto-generated offer + TILA disclosure. **PR [#12](https://github.com/2463-FDE/MC-meridian-lending/pull/12) MERGED into `main` (merge commit `6b395cb`).** ADR 0012 (Proposed), spec `docs/specs/disclosure-week4.md`. The Decimal actuarial APR fix now lives on `main` — `main:services/disclosure-service/app/apr.py` is the corrected path, no longer the add-on seed. The post-merge delivery-guard work that branch was holding shipped as PR #22 (`11bc7dd`): refuse a chain citing a non-approving decision (`fe5a4c1`) and return 409 rather than 500 on a malformed stored document (`11b6d8a`). Dana asked to automate offer/disclosure generation and said the numbers "look basically right"; `compute_apr` was using an add-on annualization instead of the Reg Z actuarial method and was **4.5pp wrong on every loan** (~36x the 0.125pp tolerance). Correctness became the deliverable. Shipped: Decimal actuarial APR + a finance charge that includes the prepaid fee; one versioned fee schedule behind a fail-closed loader (three drifted constants deleted); TILA vectors in a **blocking** CI gate; the FK-as-graph provenance chain (`disclosures` table, `offers.decision_event_id`, `v_disclosure_provenance`) **and the pipeline read that walks it**; a LangGraph maker-checker pipeline gated by a deterministic recompute; the draft -> in_review -> approved -> delivered lifecycle with reason-code routing and a Reg Z 1026.17(b) timing refusal; the underwriting UI for all of it. Live smoke run against `make up` on a populated pre-0013 volume; it found three container-only defects (see the debt note below). **Not yet run: the two LLM stages against a real key.**

---

- **Week 5** — self-serve payments: **spec package only, nothing built.** `feature/payments-week5` off `main`, ADR 0013 (Proposed, supersedes 0003), spec `docs/specs/payments-week5.md`, scoping `docs/scoping/payments-week5.md`. Dana asked to "just add a payment form" and read the three duplicate-charge tickets as customer confusion. `scripts/repro_double_charge.py` settled that against the live stack: one $100 intent sent 8 ways produced **8 charges totalling $800, all returning `200`, with only $600 credited**. The reframe: idempotency (D19) + PCI (D13, CVV retention is prohibited outright) + one publicly-routed internal endpoint that lets a caller credit a balance with no payment at all (half of D8). The reproduction also found a defect the code read missed — **D3**, an unlocked read-modify-write that loses concurrent applies, cited in code four times since the baseline but never logged and never measured. An idempotency key does not fix it; the balance mutation must become atomic. Self-serve access is designed as a `pay:loan:{id}` capability token reusing the ADR 0010 Phase B pattern, phased staff-assisted → pay-by-link → enrollment-at-sanction, so no identity programme blocks the fixes.
  - **First review round on the merged week-5 spec PR (`929943b`), 2 findings, both valid.** The one worth carrying forward: a spec can promise a behaviour its own schema forbids. D1 said a key becomes reusable after `PAYMENT_IDEMPOTENCY_TTL_HOURS`; D2 specified a permanent partial unique index on `payments(idempotency_key)` with no expiry column and no archival step — so vector R6's second insert conflicts forever and the algorithm replays a day-old response instead. The window was prose, not a mechanism. Closed by making expiry a state transition on the row: an expired key is **retired** (`idempotency_key` set to `NULL`, dropping the row out of the partial index) while the payment row itself is never deleted or archived. **Retirement is terminal-only** — an ACH payment is `submitted` for days and outlives the window, and releasing its key would both free it for a new charge while the original intent is live and destroy the value the stuck-row reaper resolves that row by. That second point was found by the teeth pass *on the fix*, not on the original spec.
  - **PR [#14](https://github.com/2463-FDE/MC-meridian-lending/pull/14) MERGED 2026-08-08 (`ad2f34e`).** 7 files / 3,168 insertions: spec, scoping, ADR 0013, debt log, `scripts/repro_double_charge.py`, its cleanup tests, and a doc-path allowlist entry. **No `services/` or `db/` file is in the merge** — the spec-only framing survives the merge, and week-5 implementation is still unbuilt.

- **Week 6** — servicing money controls (Dana asked for a rep dashboard to adjust balances and waive fees). **Merged as three PRs, 2026-08-12: #28 planning prompts (`8720d94`), #29 ADR 0014 (`0ca6f1a`), #30 comprehension report + tests (`aee1bf5`).** Deliverable was deliberately *not* the dashboard: an AI-augmented legacy-comprehension report (`docs/reports/servicing-money-comprehension-week6.md`), characterization tests pinning today's behavior (`services/servicing-service/tests/test_characterization_balance.py`), a then-red lost-update test (`tests/test_lost_update.py` — $100 + $200 concurrent applies on $500 leave $400, last writer wins), and ADR 0014 (RBAC + maker-checker + append-only ledger). The client's answers are transcribed in `docs/client-asks/week6-servicing-answers.md`; she deferred the approval workflow and asked for the authorization fix **this cycle**. **Week 6 itself shipped no behavior change**, and its fix has since landed: PR #32 merged 2026-08-13 (`f901894`), so on `main` today `adjust-balance` and `waive-fee` are role-gated (`authz.require_money_role`, `services/servicing-service/app/main.py:228` and `:245`). Of the other two, the lost update is closed and the ledger is not: `apply_payment` became one atomic statement in PR #77 (`d649bc6`, 2026-08-24), which is D3 — `adjust_balance` and `waive_fee` in the same module have since closed D32 the same way (see the Week 9 entry below) — and there is still no ledger (`db/init/001_schema.sql:122` says so in a comment; no migration adds one). The client brief's own lost-update example is wrong (payment writes `balance`, waive-fee writes `past_due` — different columns, no collision); the real one is two concurrent `apply_payment`.

- **Week 7** — payment tracing + settlement reconciliation. **Spec merged first, then built. D1–D6 are all on `main`.** The spec package was PR [#31](https://github.com/2463-FDE/MC-meridian-lending/pull/31), merged 2026-08-13 (`23c1ea1`) — `docs/specs/observability-week7.md`, `adr/0015-settlement-reconciliation-as-a-control.md`, and 9 lines mapping both into `spec-diff-gate`, with no `services/` or `db/` file. The implementation followed across #36 and #37 (the settlement read path, gated behind `X-Internal-Service` and failing closed in integer minor units instead of returning `0.0`), #39 (row-level matching rules and break classes), #41 (the D1 correlation id across payment and servicing, with #50 closing two gaps a teeth review found), #43 (exact-amount precedence over a true-window amount match), #44 (break report on stdout, human summary on stderr), #45 (the runbook procedure and exit codes), #46 and #48 (the D4 alert on per-loan absolute variance), and #47 (the cut-off pinned to UTC). #49 (`e412bc6`) accepted ADR 0015 once the control was real. `services/servicing-service/app/reconciliation.py` is 951 lines with a 1,499-line test file, and that suite is now held by the blocking `reconciliation-gate` — #54 (`9cfce81`) closed the gap, see §4.
- **Week 8** — decisioning governance and fair-lending monitoring. **Merged, with code behind it.** #35 (`c5e6359`) the monitoring spec, model card and its `services/` changes; #42 (`b303993`) ADR 0016, which fixes the export field contract; #34 (`96b95f3`) carries every Reg B principal reason to both the borrower screen and the officer screen, written in the same transaction as the decision so the reasons cannot drift from it; #38 (`1987dcd`) blocks a decision run by the applicant's own staff account (D24, **partially fixed** — pre-migration and anonymous rows stay indistinguishable, see `docs/debt-log.md`); #40 (`b6f1149`) ADR 0017 proposes maker-checker for anonymous self-submissions. Deferred by name and carded, not forgotten: `docs/cards/week8-governance.md` — G1 the adverse-action notice channel (the client deferred it; **delivery cannot be proven until it lands**) and G2 RAG-drafted wording, whose retrieval half was split out as G2a.

- **Week 9** — payment idempotency (D19). **Merged.** Schema in PR #63 (`06c27a3`, a partial unique index on `payments.idempotency_key`), capture path in PR #65 (`c0e7023`, both charge handlers claim the key insert-first via `claim_or_branch()` before the processor is contacted), held by the blocking `payment-idempotency-gate`. This is ADR 0013 Decision 1 only — Decision 2 (PAN tokenization, CVV deletion) and Decision 3 (self-serve access) are not built. **Does not fix D3**: an idempotency key stops one intent being claimed twice, it does not make the balance mutation atomic. **D3's fix merged separately on 2026-08-24** as PR #77 (`d649bc6` + characterization follow-up `bbe31d7`): migration 0019 adds `payment_applications` with `UNIQUE (payment_id)`, `apply_payment` becomes one `INSERT ... SELECT` plus an `UPDATE` computing from the stored value, `amount` leaves `ApplyPaymentIn`, and both handlers finalize to `captured` before applying so a crash cannot free the key. Held by the blocking `atomic-apply-gate`, and decided by ADR 0020, merged as PR #78 (`0d47601`). D32 is also closed: `waive_fee`'s decrement now computes from the stored `past_due` inside its own `UPDATE`, and `adjust_balance` is a compare-and-set that refuses (writes nothing) when the caller's quoted balance no longer matches the stored one — see `docs/debt-log.md` D32 for both commits.
- **Week 10** — agentic slice: policy retrieval and one root trace over the officer assistant, then native tool calling and the trace's own CI/UI closure. **Merged, code behind it.** #64 (`609605b`) recovers `search_policy` onto `main` (ADR 0019, Accepted). #66 (`9effd5a`) a host-settable LLM feature gate. #67 (`0e38c80`) the framework chat model. #68 (`92fb4fa`) one root trace id returned over the officer assistant. #69 (`d7fce80`) resyncs kb.md/CLAUDE.md through #68. #70 (`5cdc634`) keeps raw provider errors off trace spans and closes a compose-hardening-gate default-true bypass. #71 (`a04631d`) adds `policy_topic`, a closed 8-code channel the officer's question maps onto — a free-text question cannot reach the model, the boundary masks it — and enforces one search before an assistant final. #72 (`5f9362f`) carries real tool schemas and `tool_use` blocks to the provider, replacing text-encoded tool calls. #73 (`717f27c`) fixes migration 0018 closing its own `DO` block early. #74 (`1931387`) resyncs CLAUDE.md's blocking-job list with `ci.yml`. #76 (`cc84ae0`) swaps the loop to native tool calling — the framework sits in the request path with real tool calls, and refuses a tool call that arrives as text. #79 (`c6cdb46`) adds the blocking `agentic-loop-gate`; previously the whole agentic surface ran under `|| true`. #80 (`c5a11b1`) renders the trace surface in the frontend and has origination return the root trace id on the assistant response. #85 (`19ee556`) moves the trace root to the route funnel as `assistant.entry`, so a refusal raised before the loop starts — 404, ADR 0011's KYC 409, 502/503 — is a trace with an enum refusal code instead of no trace at all; the route's own pre-funnel 403/422 refusals stay untraced. #86 (`ec5013c`) adds `test_injection_resistance.py` to the blocking `agentic-loop-gate`: every inbound channel to the model is a projection to enum codes, integers and code lists. `LLM_TRACE_CONTENT` must be `false` for a graded/production run. Spec: `docs/plans/freeze-agentic-week10.md`. **Adjacent, not part of this slice:** the D3 atomic-apply fix merged the same week (#77, `ceda4e2`), and its ADR 0020 with it (#78, `0d47601`). It shares week 10's dates but belongs to week 9's payment-integrity thread.

- **After the week-10 slice** — the run continued past #86 with no further weekly deliverable, so these landed outside the week structure; the full ledger is generated into `docs/state.md`. What changes the shape of the system, each cited by what it merged as: #103 (`26d00e8`) merged `assistant_runs`, one durable row per officer assistant request — see the read-when map above, and note it is the only surface here with **no spec or ADR behind it**. #93 (`71f6f84`) merged the business outcome onto the root span. #113 (`f23e036`) merged rate limiting on the assistant routes. #123 (`3eb6bc9`) merged token metadata onto the LLM spans. #124 (`ddfb8c4`), after #118 (`245fdb7`), merged D21's close: no committed compose file may put a datastore on a host-reachable interface. #125 (`21f498e`) merged salted password hashes and append-only `audit_logs` (D27, D20). #96 (`e573401`) and #112 (`b7b720d`) merged the policy-corpus admission hardening. #111 (`588e7bd`) merged the offline evaluator (ADR 0022) and #97 (`eaeadad`) the Titan controls behind it. #91 (`49390fe`) merged the split of this page from the generated `docs/state.md`, and #94 (`fb8924f`) merged the `kb-freshness` fix to grade against the branch's merge base.
### Two standalone PRs, outside the weekly deliverables

- **PR [#11](https://github.com/2463-FDE/MC-meridian-lending/pull/11) — AI decisioning-assistant panel on the officer detail page. MERGED (`b9bfdc1`).** `feat/assistant-ui-panel`; 768 hand-written lines across 9 files (inside the 800-line cap). Surfaces the Week 3 assistant that merged on #7 — that feature now has its UI on `main`.
- **PR [#13](https://github.com/2463-FDE/MC-meridian-lending/pull/13) — reject an unreadable DOB at the boundary, not after it is stored. MERGED (`477293b`).** `fix/dob-validation`; migration `0011` adds a validated CHECK so unreadable rows cannot persist.

## 4. Open / deferred decisions (session-only — not fully captured elsewhere)

- **The reconciliation suite has no blocking CI gate — CLOSED 2026-08-22 by #54 (`9cfce81`).** Kept here so a fresh session does not re-open it. `reconciliation-gate` now runs `test_reconciliation.py`, `test_reconciliation_alert.py`, `test_reconcile_cli.py`, `test_reconciliation_utc.py` and `test_runbook_reconciliation.py` outside the matrix with no `continue-on-error` and no `|| true`. The lesson stands: the suite had run **only** inside the `backend` matrix job (`continue-on-error: true` plus `|| true`), so a regression in the double-charge detector would have shipped on a green build — the same class of mistake as the add-on-vs-actuarial APR defect, which survived because the money test that would have caught it ran under `|| true`. **What came out of the suppression on 2026-08-24:** the apply-payment path — `atomic-apply-gate` now runs `test_lost_update.py`, `test_atomic_apply.py` and `test_payment_applications_ddl.py` blocking, with no `|| true`. **What was still inside it at the time:** the remaining `balance.py` write paths, `adjust_balance` and `waive_fee` (D32) — both have since closed the same way as this apply and joined `atomic-apply-gate`; see `docs/debt-log.md` D32.
- **What a double-charged borrower is offered in the interim (PARTLY ANSWERED — read the answer before designing anything).** The operator half is decided and was decided *against* more machinery: on 2026-08-14 the client ruled the $500 on loan 4471 an open exception rather than a write-off, both processor references into the first exception report, marked for Finance Ops review, and — verbatim — *"no separate remediation or ticketing workflow is needed in this build."* So an ADR proposing a remediation or ticketing workflow contradicts a decision on file. Two caveats that keep this open: that answer was about `MISSING_IN_LEDGER` (money captured, never credited), **not** about a duplicate charge, and it says nothing borrower-facing. **Still genuinely open, and worth asking as one question:** when reconciliation finds a duplicate, is the borrower told, is a refund submitted, and who owns it. Prevention at capture now exists for the exact-retry case, which narrows this question without answering it: `payments.idempotency_key` sits under a partial unique index (#63, `06c27a3`) and both charge handlers claim it insert-first via `claim_or_branch()` before the processor is contacted (#65, `c0e7023`), held by the blocking `payment-idempotency-gate`. ADR 0013 Decision 1 is therefore built; its Decision 2 (PAN tokenization, CVV deletion) is not. A processor-side duplicate, a break that predates the fix, and a missing `processor_ref` are all still detection-only. **D3's lost update is not** — `apply_payment` became one atomic statement in #77 (`d649bc6`, merged `ceda4e2`), held by the blocking `atomic-apply-gate`; a pre-#77 D3 break stays detectable historically, but the current apply-payment path cannot produce a new one. The one thing owed from that answer **is now done**: `docs/runbooks/operations.md:175` and `:193` name Finance Ops as owner of `MISSING_IN_LEDGER` breaks, landed by #55 (`399fb73`). What stays open is only the borrower-facing half named above.
- **Who receives the reconciliation alert, when, through what channel (OPEN).** The 08-12 ask bundled this with the cut-off convention in one row; the client answered the cut-off half (processor-settled date, UTC, ±1 calendar day) and not this one, which is why it reads as answered at a glance. `docs/runbooks/operations.md:168` mails `ops@example.com`, so the control currently reaches nobody. Also open and never actually put to her: whether the card processor returns a reference at capture and in which field — the answer that would replace approximate matching with an exact key.
- **ADR 0010 — application-ownership authz (OPEN, needs product sign-off).** Anonymous serial-id IDOR across `GET /applications/{id}` (PII enumeration), `/decision` (credit pull), `/offer`, `/accept` (loan boarding). Real fix = officer-OR-owner check, but needs an applicant identity + signup flow that does not exist (gateway `/auth` is login/logout/me only, users seeded). Officer-only band-aid **rejected** — wrong policy, breaks borrower flow. Defense-in-depth landed instead (decision-state guards on offer/boarding, round 11–12) so a forged/denied app can't be boarded, but the ownership hole stays until identity lands. **LIVE EXPOSURE until then.**
- **Week 5 Q7 — a verified delivery channel (OPEN, decides launch shape, not design).** Pay-by-link needs a way to reach the borrower's contact on file. Migrations `0008` and `0009` both record that this platform has none — which is why neither backfills tokens. Phase 1 (staff-assisted, existing logins) deliberately ships without it, so the idempotency and PCI fixes are not held hostage to procurement. Seven other client questions are recorded in `docs/scoping/payments-week5.md` §6 with assumptions, so a different answer costs a config change.
- **Week 5 — CVV retention is live today.** Not a design question. Every payment since go-live holds sensitive authentication data that may not be retained after authorization. The purge is scheduled independently of, and ahead of, the form.
- **Auto-migration-runner** — pushed to own ADR/PR (ADR 0002 platform decision, all 7 services).
- **Mandatory-KYC-before-decision + fail-closed intake** — ADR 0011; product+compliance availability tradeoff.
- **Deferred lower-priority:** intra-network lateral spoof (gateway-signed token); pg/redis host-published ports.
- **Week 4 client questions still unanswered** (`docs/specs/disclosure-week4.md`, *Client Questions*): APR method of record (assumed actuarial — Reg Z prescribes it), tolerance regime (0.125pp assumed, and deliberately a config value so a different answer costs a config change), and back-book scope (the spec fixes forward; existing offers are never recomputed). None blocks the work.
- **Closure-doc carryover (opened 2026-08-04, none of it blocking).** docs/goal-weeks1-4-closure.md (local-only, un-backticked so the tracked-doc path lint skips it) is a *living* scoreboard: each of the nine trainer items carries `OPEN` / `MERGED` / `RENEGOTIATED-with-reason`, and its score line gets bumped as PRs land. Three things are still owed on it. (1) The nine are **not yet mirrored** into `feature-status-tracker.md` — that mirror is the local working checklist. (2) The `.html` twin and the published artifact (`https://claude.ai/code/artifact/5c9ed224-627f-4a35-a8f6-3c44bea8c3e6`) are **stale**: the twin was hand-wrapped, not generated, so there is no build step and every `.md` edit drifts it — republish the same path to keep the shared link, never mint a new one. (3) The tier-2 standing six (unmerged-lines-at-freeze < 1,500, PR lead time ≤ 48h, PR size ≤ 800 hand-written, ≥ 3 merges/week, deck claims 100% traceable to `main`, regulator check ≤ 7 days) outlive the closure window and move into the `CLAUDE.md` cadence section on **Mon 2026-08-10** — deferred there by the doc's own exclusion table, not forgotten.
- **Lesson from the Week 4 smoke:** a suite that runs from the repo checkout against a stub session cannot see container-layout or ORM-flush bugs. `make up` found three in one pass — an import-time `parents[3]` IndexError, an unresolvable ORM foreign key, and a NOT NULL violation from a missing server default — all with 400+ green unit tests. Treat the smoke as a gate, not a formality.

---

## 5. Gotchas

- Backend service ports are internal-only; the **gateway is the sole trust boundary**. It strips inbound `X-User-Role` / trust headers to kill spoofing. Internal-only writes require `X-Internal-Service`.
- `INTERNAL_SERVICE_TOKEN` required in kyc/decision/disclosure health; demo override supplies a dev token (`docker-compose.demo.yml`).
- `DECISION_FINGERPRINT_PEPPER` ships **blank** in `.env.example` on purpose — config rejects blank/placeholder so a copied template can't key a reversible SSN HMAC. Dev value lives only in `docker-compose.demo.yml`.
- Schema-readiness health gates: services report `schema_not_ready:<obj>` at `/health` on an unmigrated volume (blocks CI) — covers `decision_events.request_id`, its unique index, both append-only triggers, and `applications.monthly_debt`.

---

## Related sessions
- `local_6c6dca67` — Skill feature workflow integration
- `local_485a6335` — Session handoff
- `local_ca1d6570` — Plugins/skills for simple-first building

## Maintaining this file
One page. When it grows past ~2 screens, push detail into a `docs/` file and leave a pointer here. Sections 3 and 4 hold durable narrative — why a week's deliverable was reframed, what a defect cost, what is still open. They do **not** hold merge state, tip shas, counts or a synced-on date: those are generated into `docs/state.md`, and `volatile-claim-lint` fails this file if one comes back.
